/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Sricharan-portfolio',
  assetPrefix: '/Sricharan-portfolio/',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
